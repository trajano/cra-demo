language: node_js
node_js:
- node
cache:
  yarn: true
script:
- yarn test
- yarn build
deploy:
  provider: pages
  local_dir: build
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: master
